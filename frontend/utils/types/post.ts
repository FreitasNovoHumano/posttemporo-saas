/**
 * =====================================================
 * 🧠 TIPOS GLOBAIS
 * =====================================================
 */

export type Post = {
  image: string;
  title: string;
  content: string;
  cta: string;
};

export type FormData = {
  name: string;
  company: string;
  whatsapp: string;
  email: string;
};